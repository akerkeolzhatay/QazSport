<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Изменить продукт</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-dark-5@1.1.3/dist/css/bootstrap-dark.min.css" rel="stylesheet">
</head>

<body>
    <%- include('../navbar') %>
        <div class="container mt-5">
            <section class="sectionn">
            <h1 style="color: black;">Изменить продукт</h1>
            <form action="/admin/foods/<%= food._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="name" style="color: black;">Название:</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= food.name %>" required>
                </div>
                <div class="form-group">
                    <label for="variants" style="color: black;">Варианты (через запятую):</label>
                    <input type="text" class="form-control" id="variants" name="variants"
                        value="<%= food.variants.join(', ') %>" required>
                </div>
                <div class="form-group">
                    <label for="prices" style="color: black;">Цены (в формате массива JSON, например: [{"XS":5,"S":7}]):</label>
                    <input type="text" class="form-control" id="prices" name="prices"
                        placeholder='[{"small":5,"medium":7}]' required>
                </div>
                <div class="form-group">
                    <label for="category" style="color: black;">Категория:</label>
                    <input type="text" class="form-control" id="category" name="category" value="<%= food.category %>"
                        required>
                </div>
                <div class="form-group">
                    <label for="image" style="color: black;">Изображение (URL):</label>
                    <input type="text" class="form-control" id="image" name="image" value="<%= food.image %>" required>
                </div>
                <div class="form-group">
                    <label for="description" style="color: black;">Описание:</label>
                    <textarea class="form-control" id="description" name="description"
                        required><%= food.description %></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Сохранить изменения</button>
            </form>
        </section>
        </div>
</body>

</html>